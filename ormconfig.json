{
  "type": "postgres",
  "host": "localhost",
  "port": 5432,
  "username": "fourleaves",
  "password": "fourleaves",
  "database": "fourleaves",
  "entities": ["./src/models/*.ts"],
  "migrations": ["./src/database/migrations/*.ts"],
  "logging": true,
  "cli": {
    "migrationsDir": "./src/database/migrations"
  }
}
